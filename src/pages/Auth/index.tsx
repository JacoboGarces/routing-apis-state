import { ReactElement } from 'react';
import './style.css';
import { Form } from '../../components/Form';

export const Auth = (): ReactElement => {
  return <section>
    <h1>Auth</h1>
    <Form />
  </section>
}